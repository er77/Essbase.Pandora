/*VOLUMES PRODUCED ALL PLANTS*/
  SELECT   
{
 { CROSSJOIN ( CROSSJOIN (  
      {&INP_ACT_EXT}
    , {&CUR_MTH.LAG(1),&CUR_MTH} )
    , {[CU_DPC_COS_MACH]} )} 
}	
   ON COLUMNS,
 NON EMPTY NONEMPTYBLOCK      
 CROSSJOIN (
	  { INTERSECT({DESCENDANTS([CA_5000TOT])},{[WORKCENTERS].LEVELS(0).MEMBERS})}
	  ,{INTERSECT({DESCENDANTS([TOT_PROD])},{[PRODUCTS].LEVELS(0).MEMBERS})})
    ON ROWS 
  FROM RU3ACTIV.RU3ACTIV